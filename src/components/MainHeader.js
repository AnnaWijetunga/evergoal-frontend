import React from 'react';

// import Clock -- if using this
// <Clock /> -- would add this component below <h1> tag, if using

const MainHeader = () => (
    <div className="App-header">
        <h1>Evergoal</h1>
    </div>
)

export default MainHeader;